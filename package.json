{
  "name": "card-playground",
  "version": "1.0.0",
  "description": "CardPlayground — card collection game with gacha, synthesis, and social features",
  "private": true,
  "workspaces": [
    "backend",
    "client",
    "shared"
  ],
  "scripts": {
    "install:all": "npm install",
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "dev:backend": "npm run dev -w backend",
    "dev:client": "npm run dev -w client"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
